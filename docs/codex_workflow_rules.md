# Codex Workflow Rules

- After backend code changes, Codex should proactively ask whether to build a Linux binary executable for deployment.
- After backend code changes, Codex should proactively ask whether to push the changes to GitHub.
- The default Linux deployment binary path in this project is `gin-blog-linux`.
